.claude/
CLAUDE.md
__pycache__/
*.pyc
.DS_Store
node_modules/

# Code-only repo: ALL data assets kept local & out of git (videos, ELAN with
# transcribed speech, gaze, timeseries, etc. are personal/sensitive).
/interactions/
/wall_analysis/
*.mp4
*.MP4
*.mov
*.MOV
*.mp3
*.xcf

# tools/ needs upstream sources that are not distributed; the path to them is
# per-machine, like data.local.json.
tools/sources.local.json
